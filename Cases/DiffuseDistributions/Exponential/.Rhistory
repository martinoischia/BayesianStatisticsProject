library(coda)
library(BNPmix)
library(ggplot2)
library(tidyr)
library(dplyr)
library(purrr)
library(ggsci)
library(MYpackage)
require(gplots)
require(ggpubr)
set.seed(42)
# Variance is given by 1/(alpha)^{2}
data_toy <- c(rexp(15, rate=1))
model <- PYdensity(data_toy,
mcmc = list(niter = 25000, nburn = 15000,
method = "SLI", model = "LS", hyper = F))
summary(model)
ENOC(1, length(data_toy))
data.frame(x = seq(min(model$grideval), max(model$grideval), by = 0.01),
y = dexp(seq(min(model$grideval), max(model$grideval), by = 0.01), rate=1)) %>%
ggplot()+
theme_minimal() +
geom_line(aes(x = x, y = y,col = "True density")) +
geom_line(data = data.frame(xx=model$grideval,yy=colMeans(model$density)), aes(x=xx, y=yy, col="Estimated density"))+
scale_color_manual(name = "Group",
values = c( "True density" = "blue", "Estimated density" = "red"))+
geom_point(data.frame(x = data_toy),
mapping =  aes(x = x, y = 0) )
coda_model <- BNPdens2coda(model)
summary(coda_model)
x11()
plot(coda_model)
effectiveSize(coda_model)
Binder = B.loss.draws(model$clust)
library(coda)
library(BNPmix)
library(ggplot2)
library(tidyr)
library(dplyr)
library(purrr)
library(ggsci)
library(MYpackage)
require(gplots)
require(ggpubr)
set.seed(42)
# Variance is given by 1/(alpha)^{2}
data_toy <- c(rexp(15, rate=10))
model <- PYdensity(data_toy,
mcmc = list(niter = 25000, nburn = 15000,
method = "SLI", model = "LS", hyper = F))
summary(model)
ENOC(1, length(data_toy))
data.frame(x = seq(min(model$grideval), max(model$grideval), by = 0.01),
y = dexp(seq(min(model$grideval), max(model$grideval), by = 0.01), rate=10)) %>%
ggplot()+
theme_minimal() +
geom_line(aes(x = x, y = y,col = "True density")) +
geom_line(data = data.frame(xx=model$grideval,yy=colMeans(model$density)), aes(x=xx, y=yy, col="Estimated density"))+
scale_color_manual(name = "Group",
values = c( "True density" = "blue", "Estimated density" = "red"))+
geom_point(data.frame(x = data_toy),
mapping =  aes(x = x, y = 0) )
set.seed(42)
# Variance is given by 1/(alpha)^{2}
data_toy <- c(rexp(15, rate=1))
model <- PYdensity(data_toy,
mcmc = list(niter = 25000, nburn = 15000,
method = "SLI", model = "LS", hyper = F))
summary(model)
ENOC(1, length(data_toy))
data.frame(x = seq(min(model$grideval), max(model$grideval), by = 0.01),
y = dexp(seq(min(model$grideval), max(model$grideval), by = 0.01), rate=1)) %>%
ggplot()+
theme_minimal() +
geom_line(aes(x = x, y = y,col = "True density")) +
geom_line(data = data.frame(xx=model$grideval,yy=colMeans(model$density)), aes(x=xx, y=yy, col="Estimated density"))+
scale_color_manual(name = "Group",
values = c( "True density" = "blue", "Estimated density" = "red"))+
geom_point(data.frame(x = data_toy),
mapping =  aes(x = x, y = 0) )
set.seed(42)
# Variance is given by 1/(alpha)^{2}
data_toy <- c(rexp(15, rate=0.1))
model <- PYdensity(data_toy,
mcmc = list(niter = 25000, nburn = 15000,
method = "SLI", model = "LS", hyper = F))
summary(model)
ENOC(1, length(data_toy))
data.frame(x = seq(min(model$grideval), max(model$grideval), by = 0.01),
y = dexp(seq(min(model$grideval), max(model$grideval), by = 0.01), rate=0.1)) %>%
ggplot()+
theme_minimal() +
geom_line(aes(x = x, y = y,col = "True density")) +
geom_line(data = data.frame(xx=model$grideval,yy=colMeans(model$density)), aes(x=xx, y=yy, col="Estimated density"))+
scale_color_manual(name = "Group",
values = c( "True density" = "blue", "Estimated density" = "red"))+
geom_point(data.frame(x = data_toy),
mapping =  aes(x = x, y = 0) )
library(coda)
library(BNPmix)
library(ggplot2)
library(tidyr)
library(dplyr)
library(purrr)
library(ggsci)
library(MYpackage)
require(gplots)
require(ggpubr)
set.seed(42)
# Variance is given by 1/(alpha)^{2}
data_toy <- c(rexp(15, rate=0.01))
model <- PYdensity(data_toy,
mcmc = list(niter = 25000, nburn = 15000,
method = "SLI", model = "LS", hyper = F))
summary(model)
ENOC(1, length(data_toy))
data.frame(x = seq(min(model$grideval), max(model$grideval), by = 0.01),
y = dexp(seq(min(model$grideval), max(model$grideval), by = 0.01), rate=0.01)) %>%
ggplot()+
theme_minimal() +
geom_line(aes(x = x, y = y,col = "True density")) +
geom_line(data = data.frame(xx=model$grideval,yy=colMeans(model$density)), aes(x=xx, y=yy, col="Estimated density"))+
scale_color_manual(name = "Group",
values = c( "True density" = "blue", "Estimated density" = "red"))+
geom_point(data.frame(x = data_toy),
mapping =  aes(x = x, y = 0) )
coda_model <- BNPdens2coda(model)
summary(coda_model)
x11()
